# SPEC.md — kobai-walk

<!-- scaffold template v1.8.0 から展開(2026-08-17)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

2 パラメータ (w, b) の回帰モデルの損失地形を等高線として描き、同じスタート地点から
勾配降下・モメンタム・AdaGrad の 3 走者が谷底へ降りていく過程をレース形式で可視化する教材アプリ。
線形回帰では正規方程式の閉形式解と臨界学習率 2/λmax が解析的に求まるため、
「収束する・しない」の両方を厳密なオラクルで検証できる。ブラウザ完結・静的エクスポート。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | シード付き PRNG(mulberry32)を注入。データ生成は同一シードで決定論 | must |
| F-02 | データ 2 種(各 60 点・シード決定的): 線形回帰用(y = 1.6x − 0.4 + 一様ノイズ ±0.3)/ ロジスティック用(重なりのある 2 クラスタ → 最適解が有限) | must |
| F-03 | モデル: 1 特徴の線形回帰(MSE)とロジスティック回帰(BCE)。loss / grad は解析式の純関数 | must |
| F-04 | オプティマイザ 3 種: GD / Momentum(β=0.9)/ AdaGrad(ε=1e-8)。`stepOpt` は純関数・3 走者は共通の学習率とスタート | must |
| F-05 | 地形可視化: (w,b) 格子の損失を対数正規化した単色ランプで塗り、3 走者の軌跡を色分け描画(GD=青 / Momentum=橙 / AdaGrad=緑) | must |
| F-06 | レース制御: 開始 / 一時停止 / 1 ステップ / リセット・速度 1x/10x/50x | must |
| F-07 | 学習率スライダー(対数目盛 0.01〜4.0)。線形回帰では臨界学習率 2/λmax を数値表示し、超過時に警告色 | must |
| F-08 | 損失レース曲線: 3 走者の損失を対数軸で重ね描き(色+直ラベルで系列識別) | must |
| F-09 | 到達表示: 線形は正規方程式解 ★ を地形に描き、各走者の解までの距離を表示。ロジスティックは勾配ノルムを表示 | must |
| F-10 | フッタ: MIT License・GitHub・操作説明(歩き方)・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的エクスポート(`output: "export"`)。サーバ API・外部通信なし | `next build` が out/ を生成 |
| N-02 | `src/core` は純関数のみ・決定論。カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85% | eslint 境界規則 + vitest coverage 閾値 |
| N-03 | 描画(rAF)と最適化ステップを分離。1 フレームのバッチに上限 | フレーム内ステップ上限のテスト |
| N-04 | ポインタ・タッチ両対応 | 手動確認 |

## 4. 品質基準

`npm run verify` green を全ループの完了条件とする(内訳は AGENTS.md §3)。

数理ゲート(すべて解析オラクル。予算・許容誤差は較正実験で確定):

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | 勾配検証 | 解析勾配が中心差分(ε=1e-6)と相対誤差 < 1e-5 で一致(両モデル × 複数パラメータ点) |
| G-02 | 正規方程式オラクルの自己検証 | solveNormal の解で ‖∇L‖ < 1e-10、かつ地形格子の全点より損失が小さい(凸性の独立検算) |
| G-03 | GD 収束 | 線形回帰・lr = 1/λmax・500 ステップで正規方程式解との距離 < 1e-6 |
| G-04 | 臨界学習率 | lr = 1.5/λmax で損失が全ステップ単調非増加、lr = 2.2/λmax で発散(損失 > 初期値 × 10) |
| G-05 | 決定論 | 同一シード・同一設定の 2 回のレースが全軌跡で深い等値 |
| G-06 | 3 走者の合流 | Momentum・AdaGrad も線形回帰の最適解へ到達(距離 < 1e-3・予算は較正で確定) |

## 5. スコープ外

- Adam・RMSProp 等の追加オプティマイザ(将来ループ候補)
- 3 パラメータ以上のモデル(地形が描けなくなる)・ミニバッチ / SGD のノイズ
- スタート位置のドラッグ指定(固定スタート。将来ループ候補)
- 学習結果の永続化・効果音
