# TEST_SPEC.md — kobai-walk

<!-- scaffold template v1.8.0 から展開(2026-08-17) -->

## 実行規約

- `npx vitest run` を stage 3–5 の判定に使用。カバレッジ判定は `npx vitest run --coverage`
- テストは `src/core/__tests__/*.test.ts`(+ `src/lib/*.test.ts`)に置き、describe に対応 T-ID をコメントで残す
- 数値微分・正規方程式・固有値の各オラクルは**テスト内で独立に検算**する(実装の流用禁止)。
  導出前提をコメントに残す
- 収束ゲート(T-1xx)は予算付き。数値は較正実験の証拠つきでのみ確定する(VERIF-FALSE 予防)

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 | mulberry32 同一シード / 異シード | 同一列・[0,1) / 先頭 8 個不一致 |
| T-010 | F-02 | 両データセット生成(シード 1) | 各 60 点・決定的・線形は x∈[-1,1]・ロジスティックは両ラベル存在 |
| T-011 | F-02 | ロジスティックデータの重なり | 2 クラスタが重なる(= 最適解が有限)ことをラベル別 x 範囲の交差で確認 |
| T-020 | F-03 | 線形 loss の手計算一致 | 2 点の極小データで MSE を手計算と照合 |
| T-021 | F-03 | ロジスティック loss | p=0.5 相当(w=b=0)で ln2・クランプで有限 |
| T-030 | G-01 | 解析勾配 vs 中心差分(両モデル × 5 パラメータ点) | 相対誤差 < 1e-5 |
| T-040 | G-02 | solveNormal の自己検証 | ‖∇L‖ < 1e-10・格子 41×41 の全点より損失が小さい |
| T-041 | F-07 | λmax の独立検算 | 2×2 ヘッセ行列の固有値をテスト内の特性方程式で再計算し一致 |
| T-050 | F-04 | stepOpt 純関数性 | 入力非破壊・同一入力同一出力・GD 1 歩の手計算一致 |
| T-051 | F-04 | Momentum / AdaGrad の 1 歩 | 更新式 v←βv−lr·g / 蓄積 G←G+g² を手計算と照合 |
| T-060 | F-05 | lossGrid / lossColor | 格子形状・対数正規化の単調性・#rrggbb |
| T-061 | F-08 | raceCurves(対数軸 points) | kyokai-lab 同型: 正値のみ・単調写像・空列は空文字 |
| T-070 | N-03 | batchSize | 全速度で上限以下・1x は 1 |
| T-080 | F-10 | FOOTER_LINKS | 5 件・全 https・ラベル非空・歩き方≠設計図 |
| T-100 | G-03 | GD・lr=1/λmax・500 ステップ | 正規方程式解との距離 < 1e-6 |
| T-101 | G-04 | lr=1.5/λmax / 2.2/λmax | 単調非増加 / 発散(損失 > 初期 × 10) |
| T-102 | G-05 | 同一設定 2 回のレース | 全軌跡が深い等値 |
| T-103 | G-06 | Momentum・AdaGrad(予算較正) | 最適解との距離 < 1e-3 |
